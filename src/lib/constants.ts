/** Default animation duration in milliseconds for page transitions */
export const ANIMATION_DURATION = 500;

/** Spring stiffness for navigation blob animations (higher = faster) */
export const SPRING_STIFFNESS = 120;